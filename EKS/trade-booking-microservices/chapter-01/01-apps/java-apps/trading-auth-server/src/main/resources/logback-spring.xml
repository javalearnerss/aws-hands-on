<configuration>

    <property name="SERVICE_NAME" value="${SERVICE_NAME:-trading-auth-server}" />
    <property name="POD_NAME" value="${POD_NAME:-trading-auth-server}" />

    <timestamp key="DATE" datePattern="yyyy-MM-dd"/>

    <property name="LOG_PATH" value="/logs/${SERVICE_NAME}/${DATE}/${POD_NAME}" />

    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">

        <file>${LOG_PATH}/${SERVICE_NAME}.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">

            <fileNamePattern>${LOG_PATH}/${SERVICE_NAME}.%d{yyyy-MM-dd_HH}.log</fileNamePattern>

            <maxHistory>30</maxHistory>

        </rollingPolicy>

        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger - %msg%n
            </pattern>
        </encoder>

    </appender>

    <root level="INFO">
        <appender-ref ref="FILE"/>
    </root>

</configuration>