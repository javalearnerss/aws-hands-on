apiVersion: v1
kind: Service
metadata:
  name: trade-monitor-ui-service-aws
  namespace: trading-dev-aws
spec:
  type: ClusterIP
  selector:
    app: trade-monitor-ui-aws
  ports:
    - port: 80
      targetPort: 80