

1. Display only last three folders in path in cmd

    function prompt {
        $path = (Get-Location).Path -split '\\'
        $shortPath = $path[-4..-1] -join '\'
        "PS $shortPath> "
    }

--------------------------------------------------------------

2. Rollout deployment after config map is updated
    kubectl rollout restart deployment trade-monitor-ui -n trading-dev

3. Inspect a pod
    kubectl exec -it trade-monitor-ui-7c7c775c77-98bhs -n trading-dev -- sh